#!/usr/bin/python3
""" is kind of a class """


def is_kind_of_class(obj, a_class):
    """ checking for if instance """
    if (isinstance(obj, a_class)):
        return (True)
    else:
        return (False)
