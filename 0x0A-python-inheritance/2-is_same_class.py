#!/usr/bin/python3
    """ mylist class """
    def is_same_class(obj, a_class):
        """ func checks if the object is instance for the class or not """
        if type(obj) == a_class:
            return (True)
        else: 
            return (False)
